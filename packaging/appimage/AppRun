#!/bin/sh
# AppImage entrypoint: exec the native-window binary (Photino/WebKitGTK).
# No server, no browser, no ports — the window IS the app. Requires Tails 7+
# (Debian 13: glibc >= 2.38, libwebkit2gtk-4.1).
HERE="$(dirname "$(readlink -f "$0")")"
exec "$HERE/usr/bin/Slip39Demo.Desktop" "$@"
